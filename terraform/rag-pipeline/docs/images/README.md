# Images for TERRAFORM_APPLY_TO_CUSTOMERS.md

- **airflow-ui.png** — Optional screenshot of the Airflow UI showing the RAG DAGs (e.g. `sharepoint_s3_sync`, `document_pipeline`). Add when available so the playbook can reference it.
