# AWS Fargate - Full Platform

> **Status:** Planned

This guide will cover deploying the full ELIZA Accelerate platform (`APPLETS=all`) on AWS Fargate, including all applets and the complete infrastructure stack (PostgreSQL, Redis, Elasticsearch, Neo4j, Logstash, Langfuse).
